ANTHROPIC_API_KEY=
# Optional: set a shared password to gate the /queen-9k3m7q page.
# Leave blank/unset to disable the password and allow open access.
QUEEN_PASSWORD=

# --- Newsletter ---------------------------------------------------------
# Postgres connection string (Neon / Vercel Postgres).
DATABASE_URL=
# Resend API key.
RESEND_API_KEY=
# Sender, on a domain verified in Resend (Domains > Add, then the DNS records).
# e.g. Morgan Collado <notes@morgancollado.com>
NEWSLETTER_FROM=
# Gates /newsletter/admin. Unlike QUEEN_PASSWORD, leaving this blank LOCKS
# the page rather than opening it — the console can email real people.
NEWSLETTER_ADMIN_PASSWORD=
