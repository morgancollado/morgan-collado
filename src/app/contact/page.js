import ContactPage from "@/components/contact-page";

export const metadata = {
  title: "Contact | Morgan Collado",
  description:
    "Letters to the editor — where to find Morgan Collado on the networks.",
};

export default function Contact() {
  return <ContactPage />;
}
